{htxt id='civi_flexmailer_embedimages'}
{ts}Enable if you want images to be embedded into the email (inline images), not referenced as a link.
  This increases the email size but displays images in the client right away.{/ts}
{/htxt}
{htxt id='civi_flexmailer_embedimageslocal'}
{ts}If embedding is enabled, setting to yes embeds only locally hosted images.{/ts}
{/htxt}
